{
  programs.foot = {
    enable = true;

    settings = {
      main = { pad = "20x20"; }; 
      mouse = { hide-when-typing = "yes"; };
      tweak = { font-monospace-warn = "no"; };
    
      };
  };
}
