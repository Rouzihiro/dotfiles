filekinds:
  normal: { foreground: "{{fg:hex}}" }
  directory: { foreground: "{{accent:hex}}" }
  symlink: { foreground: "{{color6:hex}}" }
  pipe: { foreground: "{{color8:hex}}" }
  block_device: { foreground: "{{warning:hex}}" }
  char_device: { foreground: "{{warning:hex}}" }
  socket: { foreground: "{{color8:hex}}" }
  special: { foreground: "{{color5:hex}}" }
  executable: { foreground: "{{success:hex}}" }
  mount_point: { foreground: "{{color14:hex}}" }

perms:
  user_read: { foreground: "{{color6:hex}}" }
  user_write: { foreground: "{{color5:hex}}" }
  user_execute_file: { foreground: "{{success:hex}}" }
  user_execute_other: { foreground: "{{success:hex}}" }
  group_read: { foreground: "{{color6:hex}}" }
  group_write: { foreground: "{{warning:hex}}" }
  group_execute: { foreground: "{{success:hex}}" }
  other_read: { foreground: "{{color6:hex}}" }
  other_write: { foreground: "{{color9:hex}}" }
  other_execute: { foreground: "{{success:hex}}" }
  special_user_file: { foreground: "{{color9:hex}}" }
  special_other: { foreground: "{{error:hex}}" }
  attribute: { foreground: "{{color8:hex}}" }

size:
  major: { foreground: "{{color6:hex}}" }
  minor: { foreground: "{{color5:hex}}" }
  number_byte: { foreground: "{{fg:hex}}" }
  number_kilo: { foreground: "{{color6:hex}}" }
  number_mega: { foreground: "{{color14:hex}}" }
  number_giga: { foreground: "{{warning:hex}}" }
  number_huge: { foreground: "{{color9:hex}}" }
  unit_byte: { foreground: "{{fg:hex}}" }
  unit_kilo: { foreground: "{{color14:hex}}" }
  unit_mega: { foreground: "{{color6:hex}}" }
  unit_giga: { foreground: "{{warning:hex}}" }
  unit_huge: { foreground: "{{color9:hex}}" }

users:
  user_you: { foreground: "{{color4:hex}}" }
  user_root: { foreground: "{{color5:hex}}" }
  user_other: { foreground: "{{color6:hex}}" }
  group_yours: { foreground: "{{color14:hex}}" }
  group_root: { foreground: "{{color5:hex}}" }
  group_other: { foreground: "{{fg:hex}}" }

links:
  normal: { foreground: "{{color14:hex}}" }
  multi_link_file: { foreground: "{{color6:hex}}" }

git:
  new: { foreground: "{{success:hex}}" }
  modified: { foreground: "{{color5:hex}}" }
  deleted: { foreground: "{{error:hex}}" }
  renamed: { foreground: "{{color6:hex}}" }
  typechange: { foreground: "{{color6:hex}}" }
  ignored: { foreground: "{{color8:hex}}" }
  conflicted: { foreground: "{{warning:hex}}" }

git_repo:
  branch_main: { foreground: "{{color8:hex}}" }
  branch_other: { foreground: "{{color14:hex}}" }
  git_clean: { foreground: "{{bg:hex}}" }
  git_dirty: { foreground: "{{color5:hex}}" }

file_type:
  image: { foreground: "{{color14:hex}}" }
  video: { foreground: "{{color6:hex}}" }
  music: { foreground: "{{success:hex}}" }
  lossless: { foreground: "{{color6:hex}}" }
  crypto: { foreground: "{{error:hex}}" }
  document: { foreground: "{{fg:hex}}" }
  compressed: { foreground: "{{warning:hex}}" }
  temp: { foreground: "{{color8:hex}}" }
  compiled: { foreground: "{{color8:hex}}" }
  build: { foreground: "{{success:hex}}" }
  source: { foreground: "{{color5:hex}}" }

punctuation: { foreground: "{{color8:hex}}" }
date: { foreground: "{{warning:hex}}" }
inode: { foreground: "{{color8:hex}}" }
blocks: { foreground: "{{color8:hex}}" }
header: { foreground: "{{fg:hex}}" }
octal: { foreground: "{{warning:hex}}" }
flags: { foreground: "{{color5:hex}}" }

symlink_path: { foreground: "{{color14:hex}}" }
control_char: { foreground: "{{warning:hex}}" }
broken_symlink: { foreground: "{{color9:hex}}" }
broken_path_overlay: { foreground: "{{color9:hex}}" }
