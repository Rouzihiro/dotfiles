@import "colors.rasi"

configuration {
    show-icons: true;
    icon-theme: "Numix-Circle";
    font: "JetBrainsMono Nerd Font 11";
}

* {
    background-color: transparent;
    text-color: @foreground;
}

window {
    background-color: @sigbg;  /* Softer dark background */
    border: 2px solid;
    border-color: @sig1;      /* Warm golden border instead of bright orange */
    border-radius: 12px;
    padding: 12px;
    width: 700px;
}

mainbox {
    background-color: transparent;
    children: [ inputbar, listview ];
    spacing: 12px;
}

inputbar {
    background-color: @sigbg;
    border-radius: 8px;
    padding: 8px 12px;
    children: [ prompt, entry ];
    spacing: 8px;
}

prompt {
    text-color: @sig1;        /* Golden prompt for hierarchy */
    font: "JetBrainsMono Nerd Font Bold 12";
}

entry {
    placeholder: "Search...";
    placeholder-color: @sigbg;
    text-color: @foreground;
}

listview {
    background-color: transparent;
    columns: 4;
    lines: 3;
    spacing: 6px;
    scrollbar: false;
    fixed-height: false;
    border: 0;
}

element {
    background-color: @sigbg;
    text-color: @foreground;
    border-radius: 8px;
    padding: 8px 8px;
    spacing: 8px;
    orientation: vertical;
    children: [ element-icon, element-text ];
}

element-icon {
    size: 96px;
    horizontal-align: 0.5;
}

element-text {
    horizontal-align: 0.5;
    text-color: inherit;
}

element selected {
    background-color: @sig1;  /* Calm blue instead of bright orange */
    text-color: @sigbg;       /* Light text for contrast */
}

element selected.active {
    background-color: @sig1;  /* Golden active state */
    text-color: @sigbg;        /* Dark text for readability */
}

element-text selected {
    text-color: inherit;
}
